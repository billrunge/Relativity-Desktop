<UserControl x:Class="RelativityDesktop.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RelativityDesktop.Views"
             mc:Ignorable="d"
             Background="White"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- Row 0 (Buttons) -->

        <Button Name="WorkspaceButton" Grid.Row="0" Grid.Column="1">Workspace</Button>
        <Button Name="UserButton" Grid.Row="0" Grid.Column="2">Users</Button>
        <Button Name="GroupButton" Grid.Row="0" Grid.Column="3">Groups</Button>
        
        <!-- Row 1 (Content) -->

        <ContentControl x:Name="ActiveItem"  Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3"/>



    </Grid>
</UserControl>
